apiVersion: v1
kind: Service
metadata:
  name: sandbox-app
spec:
  selector:
    app: sandbox-app
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: metrics
      port: 9100
      targetPort: metrics
