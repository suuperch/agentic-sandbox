# Plan for #7: Add readiness and liveness probes, run two replicas

## Goal
The deployment routes traffic only to ready pods and keeps a rolling update resilient by probing the health endpoint and running two replicas.

## Dependencies (confirmed / plausible / open), each with change: yes | no
- Confirmed: `k8s/deployment.yaml` defines the Deployment and container ports; change: yes.
- Confirmed: `k8s/service.yaml` exposes the app and depends on the deployment labels; change: no.
- Confirmed: `src/server.ts` exposes `GET /health`; change: no.
- Confirmed: `README.md` documents the app runtime and health endpoint in general; change: no.
- Open: none.

## Tests
- `npm run verify` for the local project sanity check.
- Pipeline rollout/stability/smoke/browser checks for the deployment.

## Environment contract
- No new or changed environment variables; this change uses the existing `/health` endpoint.

## Risks
- A wrong path or port on the probes keeps pods unready and blocks rollout.
- Increasing replicas without stable health checks can cause traffic to hit not-ready pods.

## Open questions
1. None.

## Decisions
- No additional decision needed; the ticket is explicit and the health endpoint already exists.
